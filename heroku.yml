setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE

build:
  docker:
    web: Dockerfile
  config:
    # NUXT_HOST: 0.0.0.0
    # NUXT_PORT: "%env(PORT)%"

run:
  web: npm run build && npm run start